import CommonFullWidthWrapper from '@components/v2/common-full-width-wrapper';
import styled from '@emotion/styled';

export const BlackSectionEndWrapper = styled(CommonFullWidthWrapper)`
  position: absolute;
  bottom: 0;
`;
